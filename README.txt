you will have to install the module for the slash commands.
To do this open a cmd and type in "pip install discord-py-slash-command".
After u done this, you will have to import the module like in the file